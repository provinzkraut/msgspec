rules:
  dangerous-triggers:
    ignore:
    # The trusted default-branch policy reads the proposed merge as data.
    - ci-capability-policy.yml
    # Trusted orchestration isolates the untrusted docs build from deployment.
    - docs-deploy.yml
